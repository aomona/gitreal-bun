# gitreal-bun

Bun (TypeScript) reimplementation of `watany-dev/gitreal`, aiming for full CLI compatibility on macOS and Linux.

## Requirements
- Bun

## Development
- Install deps: `bun install`
- Run: `bun run src/index.ts --help`

## Install (local)
- `bun link`
- `gitreal --help`